# Requires Python >= 3.10 (uses X | Y union type syntax)

numpy==2.2.6
requests==2.33.0  # CVE-2026-25645 fixed in 2.33.0 (extract_zipped_paths temp-file race)
pandas==2.3.3
pyreadr==0.5.3
scipy==1.15.2
matplotlib>=3.8,<4.0
defusedxml>=0.7,<1.0

# Test dependencies
pytest>=8.0,<10.0
